// import Schema from '../Schemas/user.schema';

export default () => {
    // let params = {
    //     name: 'John Doe',
    //     email: 'john@doe.com',
    //     role: 'admin'
    // };

    // Schema.find(params).exec((error, user) => {
    //     if (error) throw error;
    //     if (user.length == 0) {
    //         params.password = '1234';
    //         let schema = new Schema(params);
    //         schema.save(err => {
    //             if (err) throw err;
    //         });
    //     }
    // });
}
