machine:
  node:
    version: 6.4.0
test:
  pre:
    - npm install -g karma karma-jasmine karma-spec-reporter jasmine jasmine-core jasmine-spec-reporter phantomjs karma-phantomjs-launcher
  override:
    - karma start --single-run
